@page
@model PetShop_Atlantico.Pages.Accommodations.IndexModel

@{
    ViewData["Title"] = "Accommodations";
}

<h1>Accommodations</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Accommodation[0].IsVacant)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Accommodation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IsVacant)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
